{
  "name": "web-proxy",
  "version": "1.0.0",
  "description": "A Cloudflare Worker that proxies arbitrary web requests",

  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "publish": "wrangler publish"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "web",
    "proxy"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
